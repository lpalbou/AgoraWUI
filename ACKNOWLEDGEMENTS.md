# Acknowledgements

Agora WUI ports the user-designated current Teams experience from the `abstractcontinuum` source tree. The port preserves the interaction model and visual baseline while replacing framework UI, gateway, and runtime dependencies with local React primitives and native Agora Hub transport.

The Agora project supplies the collaboration protocol and Hub service. `agora-tui` is the terminal companion to this web package.
